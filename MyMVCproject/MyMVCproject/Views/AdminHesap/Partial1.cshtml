@model myfirstproject.Models.siniflar.Admin

@using (Html.BeginForm("AdminBilgiGuncelle", "AdminHesap", FormMethod.Post))
{
    <form class="form-horizontal">
        @*<div class="form-group row">
                <label for="inputName" class="col-sm-2 col-form-label">ID</label>
                <div class="col-sm-10">
                    @Html.TextBoxFor(x => x.Cariid, new { @class = "form-control" })
                </div>
            </div>*@
        @Html.HiddenFor(x => x.AdminId)
        <div class="form-group row">
            <label for="inputName" class="col-sm-2 col-form-label">Kullanıcı Adı</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(x => x.KullaniciAd, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group row">
            <label for="inputEmail" class="col-sm-2 col-form-label">Şifre</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(x => x.Sifre, new { @class = "form-control" })
            </div>
        </div>
        
        <div class="form-group row">
            <div class="offset-sm-2 col-sm-10">
                <div class="checkbox">
                    <label>
                        <input type="checkbox"> Şifremi değiştirmek istiyorum
                    </label>
                </div>
            </div>
        </div>
        <div class="form-group row">
            <div class="offset-sm-2 col-sm-10">
                <button type="submit" class="btn btn-danger">Güncelle</button>
            </div>
        </div>
    </form>
}

